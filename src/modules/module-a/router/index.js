import Login from '../views/login'
import Page1 from '../views/page1'

let route = [
    {
        path: '/login',
        name: 'login',
        component: Login,
    },
    {
        path: '/page1',
        name: 'page1',
        component: Page1,
    }
]

export default route
